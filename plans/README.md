# Implementation Plans

Generated by the improve skill on 2026-06-11, against commit `8c47034`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Fix `respectReducedMotion: false` one-way no-op (+ first reduced-motion tests) | P1 | S | — | DONE (branch `advisor/001-fix-reduced-motion-opt-out`, commit `dccac65`, merged to main 2026-06-11) |
| 002 | Fix `init({ muted: false })` unable to unmute | P1 | S | 001 (soft — same file, sequencing only) | DONE (branch `advisor/002-fix-muted-option-asymmetry`, commit `252fb1a`, includes 001, merged to main 2026-06-11) |
| 003 | Clear npm audit (3 dev-dep vulns, 1 critical in vitest) | P1 | S | — | DONE (branch `advisor/003-dev-dependency-audit-fix`, commit `219e206`, merged to main 2026-06-11) |
| 004 | Release automation: demo-pin sync script + tag-triggered publish with provenance | P2 | M | — (land after 001–003 so first automated release includes fixes) | DONE (branch `advisor/004-release-automation`, commit `3947f08`, merged to main 2026-06-11) |
| 005 | Add CLAUDE.md (commands, architecture, Web Audio invariants) | P2 | S | best after 001/002/004 (documents their semantics) | DONE (branch `advisor/005-add-claude-md`, commit `226dfc2`, includes 001+002+004, merged to main 2026-06-11) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Execution record (2026-06-11)

All five plans were executed by subagents in isolated worktrees, approved on review, and **merged to `main`** (fast-forward to `226dfc2`, then merge commit `13bc812` for plan 003). Post-merge verification: typecheck clean, 79/79 tests, `npm audit` 0 vulnerabilities, build clean. Advisor branches and agent worktrees deleted after merge.

Outstanding human action: configure the `NPM_TOKEN` repo secret per `RELEASING.md` before the first tag-triggered release.

## Dependency notes

- 002 after 001: both edit `AudioEngine.init()` in `src/engine.ts`; sequencing avoids conflicts. Not a logical dependency.
- 005 last: CLAUDE.md should document the post-fix option semantics (001/002) and point at `RELEASING.md` (004).
- 004's publish step stays inert until a human configures the `NPM_TOKEN` repo secret (documented inside the plan).

## Audited but not planned (by user selection, 2026-06-11)

Findings confirmed in the audit that the maintainer chose not to plan yet —
fair game for a future run:

- **Gesture-unlock dead-end** (`src/engine.ts:97-117`): if `resume()` resolves while the context is in a non-`running` state (e.g. iOS `interrupted`), the `unlocking` flag is never reset and the unlock listeners go permanently inert. MED confidence; `playSound`'s own resume retry partially masks it. Fix: reset `unlocking = false` on the resolve-but-not-running path.
- **No lint/format tooling**: no ESLint/Biome/Prettier anywhere; CI gates only typecheck+test.
- **`toggleOn`/`toggleOff` duplication** (`src/generators/toggle.ts`): two ~30-line blocks differing in two frequency constants.
- **Generator tests assert only no-throw** (`src/tests/generators.test.ts`): envelope values and scheduling are unasserted, so tuning regressions pass CI silently.

## Direction options surfaced (not planned)

- Declarative `data-tiks` attribute auto-binding (~300B delegation layer) for the CDN/no-build audience (README:176-181).
- Built-in hover throttling (productizes the manual debounce advice at README:51-54).
- Svelte/Solid adapters (Vue adapter came from an external contributor — demand signal; `react.ts` is a 42-line template).
- More built-in theme presets (12-line objects; demo doubles as gallery).

## Findings considered and rejected

- React hook ignoring same-name custom-theme object changes (`src/react.ts:13-15`): documented by-design tradeoff in a code comment — re-init keyed on theme name to avoid per-render churn. Not a bug.
- Whole-library "envelope helper" refactor across all 9 generators: regression risk to sound character exceeds payoff at ~600 source lines, and there are no audio-level tests to catch it. Only the toggle pair (above) is worth deduplicating.
- Vue composable comment says "init once on mount" but init runs during `setup()`: behaviorally fine (SSR-safe — engine guards on `window`), trivial comment nit.
- Honoring `prefers-reduced-motion` by auto-muting flagged as "surprising behavior": intentional, documented accessibility default (README Accessibility section), not a finding.
